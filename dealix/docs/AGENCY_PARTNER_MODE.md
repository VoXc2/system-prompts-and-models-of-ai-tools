# Agency Partner Mode

وضع للوكالات: شراكات، عميل تجريبي مشترك، Proof مشترك، تتبع إحالات.

## المنتج

- خدمات: `partner_sprint`, `agency_partner_program` في Service Tower.
- المشغّل: `mode=agency_partner` في `POST /api/v1/operator/chat/message` (يؤثر على `mode_profile` فقط في MVP).

## الصفحة

- [`landing/agency-partner.html`](../landing/agency-partner.html)
